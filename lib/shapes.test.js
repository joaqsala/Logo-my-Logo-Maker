// Constructor Shape is imported.
const Figures = require('.shapes.js');

// A testing suite for Shapes is created.
describe('Shape', () => {
  // A test is created to check that a circle is produced when circle is chosen.
    describe('circle', () => {
    it('should produce a circle', () => {
        const total = 4;
        const arithmetic = new Calculator(2, 2, "sum");
        expect(arithmetic.sum()).toEqual(total);
    });
    });
});
